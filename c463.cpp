#include <bits/stdc++.h>
using namespace std;

vector<int> node;

struct Tree{
    int node;
    Tree nextA;
} tree;


int main(){
    int n, k;
    cin >> n;
    for (int i = 1; i <= n; i++){
        cin >> k;
        if (k > 0){

        }
        else
            node.push_back(i);
    }
}